# Plan Template

> Hybrid OpenSpec note: the step-by-step implementation breakdown for this slice is owned by
> `openspec/changes/<NN-name>/tasks.md` (and the rationale by `design.md`), authored via
> `/opsx:propose` and worked via `/opsx:apply`. This `plan.md` is a thin delivery-side anchor
> that captures the Progress Snapshot, the outcome/dependency framing, and risks. Do NOT
> duplicate the OpenSpec task list here — REFERENCE it. When they disagree, the OpenSpec change
> wins for task content; `.agents/references/assessment-decisions.md` wins for locked decisions.

## OpenSpec Change

- Change ID: `openspec/changes/<NN-name>/`
- Tasks (authoritative): `openspec/changes/<NN-name>/tasks.md`
- Design (rationale): `openspec/changes/<NN-name>/design.md`

## Progress Snapshot

- Status:
- Current Branch:
- Last Updated:
- Current Step:
- Next Step:
- Validation State:
- PR/Merge State:

## Outcome

- Target:
- Dependencies: (prior slices, e.g. auth before upload; running Redis/Postgres/Chroma)

## Steps

> High-level delivery checkpoints only. The granular checklist lives in
> `openspec/changes/<NN-name>/tasks.md`.

1. Step:
   - Validation:
   - Expected artifact:

2. Step:
   - Validation:
   - Expected artifact:

3. Step:
   - Validation:
   - Expected artifact:

## Risks

- Risk:
- Mitigation:
